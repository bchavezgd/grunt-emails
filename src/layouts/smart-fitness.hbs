{{> sf-head }}
	{{> preheader }}


		{{> sf-front-matter }}

		{{ body }}
		
		{{> sf-featured-article }}

		{{#if sf-vars.featured.ready}}
			{{> sf-featured-trainer-2col }}
		{{/if}}
		
		{{> sf-banner }}


		
    	{{> sf-blog-loop }}

		{{> webinar }}

		{{> sf-footer }}
{{> foot }}

